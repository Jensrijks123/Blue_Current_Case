const vorigeButton = document.querySelector('.vorige-button');

// Check if input is checked, only then go to the next page
vorigeButton.addEventListener('click', ()=>{
    window.location.href = "index.html";
});
